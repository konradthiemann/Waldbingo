# ─── Python ───
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/

# Virtuelle Umgebungen
.venv/
venv/
env/

# ─── Generierte PDF-Artefakte (werden per `python waldbingo.py` neu erzeugt) ───
/waldbingo.pdf
*-test.pdf

# ─── Node / Vite + React (waldbingo-app) ───
node_modules/
dist/
.vite/
*.tsbuildinfo

# ─── Lokale Secrets / Umgebungsvariablen (niemals committen) ───
.env
.env.*
!.env.example

# ─── Einladungs-Server: lokal gespeicherte Spiele (TTL-basiert, nicht versioniert) ───
waldbingo-app/server/games-data/

# ─── Claude Code lokale Tooling-Konfiguration (Preview-Server etc.) ───
.claude/

# ─── Betriebssystem / Editor ───
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Hinweis: objects.json und objects.data.js sind absichtlich versioniert.
# Sie sind generierte, aber konsumierte Datenquellen; die CI prüft, dass sie
# exakt mit build_data.py synchron bleiben (Job "Daten-Validierung & Sync").
